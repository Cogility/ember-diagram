import eventToSVGLocal from '../../../utils/event-to-svglocal';
import { module, test } from 'qunit';

module('eventToSVGLocal');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eventToSVGLocal();
  assert.ok(result);
});
