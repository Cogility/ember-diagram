import eventToSVGLocal from 'ember-diagram/utils/event-to-svglocal';
import { module, test } from 'qunit';

module('eventToSVGLocal');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.expect(0);   // until the test is implemented
  // This needs to set up an SVG element to test within, probably needs to
  // use the component integration test module to do that.
  //
  // var result = eventToSVGLocal();
  // assert.ok(result);
});
