import DragGesture from 'ember-diagram/utils/drag-gesture';
import { module, test } from 'qunit';

module('dragGesture');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = DragGesture.create();
  assert.ok(result);
});
