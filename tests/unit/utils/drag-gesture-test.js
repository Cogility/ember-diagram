import dragGesture from '../../../utils/drag-gesture';
import { module, test } from 'qunit';

module('dragGesture');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dragGesture();
  assert.ok(result);
});
