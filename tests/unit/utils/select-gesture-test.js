import selectGesture from '../../../utils/select-gesture';
import { module, test } from 'qunit';

module('selectGesture');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = selectGesture();
  assert.ok(result);
});
